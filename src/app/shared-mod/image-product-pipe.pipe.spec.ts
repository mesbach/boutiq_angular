import { ImageProductPipePipe } from './image-product-pipe.pipe';

describe('ImageProductPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ImageProductPipePipe();
    expect(pipe).toBeTruthy();
  });
});
